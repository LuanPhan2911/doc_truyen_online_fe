import Dropdown from "../../../../components/Dropdown";

const Ranking = () => {
  const options = ["one", "two", "three"];
  return <Dropdown title={"Bang xep hang"} options={options} />;
};
export default Ranking;
