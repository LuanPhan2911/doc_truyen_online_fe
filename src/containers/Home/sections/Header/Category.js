import Dropdown from "../../../../components/Dropdown";

const Category = () => {
  const options = ["one", "two", "three"];
  return <Dropdown title={"The loai"} options={options} />;
};
export default Category;
